# Copy to .env.infra and fill in existing deployment values. Never commit that file.
SITE_AWS_ACCOUNT_ID=
SITE_HOSTED_ZONE_ID=
SITE_BUCKET_NAME=
SITE_ROUTING_FUNCTION_NAME=
SITE_DEPLOY_ROLE_NAME=
# Optional: select a local AWS credential profile. Credentials stay in AWS tooling.
# AWS_PROFILE=
